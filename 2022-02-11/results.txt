The runs that sum to 3 are:

[[1, 2]]

===================================================

The runs that sum to 4 are:

[]

===================================================

The runs that sum to 5 are:

[[2, 2]]

===================================================

The runs that sum to 6 are:

[[1, 3]]

===================================================

The runs that sum to 7 are:

[[3, 2]]

===================================================

The runs that sum to 8 are:

[]

===================================================

The runs that sum to 9 are:

[[2, 3], [4, 2]]

===================================================

The runs that sum to 10 are:

[[1, 4]]

===================================================

The runs that sum to 11 are:

[[5, 2]]

===================================================

The runs that sum to 12 are:

[[3, 3]]

===================================================

The runs that sum to 13 are:

[[6, 2]]

===================================================

The runs that sum to 14 are:

[[2, 4]]

===================================================

The runs that sum to 15 are:

[[1, 5], [4, 3], [7, 2]]   ********

===================================================

The runs that sum to 16 are:

[]

===================================================

The runs that sum to 17 are:

[[8, 2]]

===================================================

The runs that sum to 18 are:

[[3, 4], [5, 3]]

===================================================

The runs that sum to 19 are:

[[9, 2]]

===================================================

The runs that sum to 20 are:

[[2, 5]]

===================================================

The runs that sum to 21 are:

[[1, 6], [6, 3], [10, 2]]   ********

===================================================

The runs that sum to 22 are:

[[4, 4]]

===================================================

The runs that sum to 23 are:

[[11, 2]]

===================================================

The runs that sum to 24 are:

[[7, 3]]

===================================================

The runs that sum to 25 are:

[[3, 5], [12, 2]]

===================================================

The runs that sum to 26 are:

[[5, 4]]

===================================================

The runs that sum to 27 are:

[[2, 6], [8, 3], [13, 2]]   ********

===================================================

The runs that sum to 28 are:

[[1, 7]]

===================================================

The runs that sum to 29 are:

[[14, 2]]

===================================================
